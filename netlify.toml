[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: unknown paths under /trombone/ serve the app so a deep link
# (if routing is ever added) survives a refresh instead of 404ing.
[[redirects]]
  from = "/trombone/*"
  to = "/trombone/index.html"
  status = 200
